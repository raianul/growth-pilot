export default function ResultsPage(_props: {
  query: string; postcode?: string;
  onSelectRestaurant: (slug: string) => void;
  onBack: () => void;
  onNewSearch: (query: string, postcode?: string) => void;
}) {
  return <div>Results — TODO Task 6</div>;
}
